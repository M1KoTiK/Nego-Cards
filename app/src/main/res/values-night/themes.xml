<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NegoCards" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/black18</item>
        <item name="android:colorBackground">@color/black30</item>
        <item name="colorPrimary">@color/black18</item>
        <item name="popupMenuStyle">@style/CustomPopupBackground</item>
    </style>
</resources>